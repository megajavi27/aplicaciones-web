@model IEnumerable<EvaluacionParcial1.Models.Profesor>
@{ ViewData["Title"] = "Profesores"; }

<div class="mb-6 flex justify-between items-center">
    <div>
        <h1 class="text-3xl font-bold text-slate-800">Profesores</h1>
        <p class="text-slate-500">Gestión de personal docente</p>
    </div>
    <a asp-action="Create" class="bg-indigo-600 hover:bg-indigo-700 text-white font-medium py-2 px-5 rounded-lg shadow-sm transition duration-200 flex items-center gap-2">
        <i class="bi bi-plus-lg"></i> Nuevo Profesor
    </a>
</div>

<div class="bg-white shadow-sm rounded-xl overflow-hidden border border-slate-200">
    <table class="w-full text-left border-collapse">
        <thead>
            <tr class="bg-gradient-to-r from-indigo-600 to-violet-600 text-white">
                <th class="py-4 px-6 font-semibold">@Html.DisplayNameFor(model => model.Nombre)</th>
                <th class="py-4 px-6 font-semibold">@Html.DisplayNameFor(model => model.Apellido)</th>
                <th class="py-4 px-6 font-semibold">@Html.DisplayNameFor(model => model.Especialidad)</th>
                <th class="py-4 px-6 font-semibold">@Html.DisplayNameFor(model => model.Email)</th>
                <th class="py-4 px-6 font-semibold text-center rounded-tr-xl">Acciones</th>
            </tr>
        </thead>
        <tbody class="divide-y divide-slate-100">
            @foreach (var item in Model) {
                <tr class="hover:bg-slate-50 transition duration-150">
                    <td class="py-3 px-6">@Html.DisplayFor(modelItem => item.Nombre)</td>
                    <td class="py-3 px-6">@Html.DisplayFor(modelItem => item.Apellido)</td>
                    <td class="py-3 px-6">
                        <span class="bg-indigo-50 text-indigo-700 px-3 py-1 rounded-full text-sm font-medium border border-indigo-100">@item.Especialidad</span>
                    </td>
                    <td class="py-3 px-6 text-slate-500">
                        <a href="mailto:@item.Email" class="hover:text-indigo-600 transition">@item.Email</a>
                    </td>
                    <td class="py-3 px-6 flex justify-center gap-2">
                        <a asp-action="Edit" asp-route-id="@item.ProfesorId" class="text-blue-600 hover:text-blue-800 bg-blue-50 hover:bg-blue-100 p-2 rounded-lg transition" title="Editar">
                            <i class="bi bi-pencil-square"></i>
                        </a>
                        <a asp-action="Details" asp-route-id="@item.ProfesorId" class="text-indigo-600 hover:text-indigo-800 bg-indigo-50 hover:bg-indigo-100 p-2 rounded-lg transition" title="Ver Detalles">
                            <i class="bi bi-eye"></i>
                        </a>
                        <a asp-action="Delete" asp-route-id="@item.ProfesorId" class="text-red-600 hover:text-red-800 bg-red-50 hover:bg-red-100 p-2 rounded-lg transition" title="Eliminar">
                            <i class="bi bi-trash"></i>
                        </a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>
