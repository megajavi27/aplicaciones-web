info:
  name: /api/Auth/logout
  type: http
  seq: 3

http:
  method: POST
  url: https://localhost:7160/api/Auth/logout
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
